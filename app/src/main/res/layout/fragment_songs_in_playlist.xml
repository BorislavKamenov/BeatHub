<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/very_dark_blue">

    <RelativeLayout
        android:id="@+id/current_selected_playlist_relative_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/current_album_selector">

        <TextView
            android:id="@+id/current_playlist_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textColor="@color/light_blue"
            android:textStyle="bold"
            android:layout_marginLeft="5dp"
            android:layout_marginTop="5dp"
            android:lines="1"/>

        <TextView
            android:id="@+id/count_of_songs_in_current_playlist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="17sp"
            android:layout_alignLeft="@id/current_playlist_name"
            android:layout_below="@id/current_playlist_name"
            android:textColor="@color/light_light_gray"/>

        </RelativeLayout>

    <ListView
        android:id="@+id/songs_in_playlist_listview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/current_selected_playlist_relative_layout"/>

</RelativeLayout>
